---
description: 微信小程序 / uni-app（mp-weixin）兼容要求，编写与修改代码时必须遵守
globs: src/**/*.vue
alwaysApply: false
---

# 微信小程序兼容（重要）

本项目使用 **uni-app 编译到微信小程序（mp-weixin）**。实现或修改功能时，默认以 **真机与基础库表现为准**，不能只在微信开发者工具里“能跑”就认为完成。

## 布局与滚动

- **`scroll-view`（尤其纵向）**：避免依赖 `height: 0` + `flex: 1` 且不给出父级明确高度；父级使用 **`min-height`/`height`（如 `100vh`）** 等可追溯高度，再给子级 `flex: 1`。
- **嵌套滚动**：注意小程序对嵌套 `scroll-view` 的限制与性能，优先设计单一主滚动区。
- **`scroll-left` 等属性**：避免向 WXML 写入小程序不接受的类型；慎用 `undefined`/非常规值；条件不绑定时查文档与多端表现。

## 模板与节点

- **列表渲染**：优先使用 **`<block v-for>`**（或单根 `view`），避免使用在小程序端易出问题的 **`<template v-for>`** 写法。
- **属性**：不写小程序/WXML 不认或非必要的 DOM 属性（例如 **`aria-*`**、`role` 等），除非确认目标端支持。

## 成片列表（`pages/create/works`）

- **`getFinishedVideoList`**：在真实接口未接好前，**非 `mock: true` 时固定返回空数组**；真机若从非「生成页」入口进入且未带 `?mock=1`、又不是开发构建，会看到「暂无成片」或仅顶栏（若再配合滚动区高度问题会更像白屏）。联调后改为走真实接口；需验空列表时用 **`?mock=0`**。
- **页面根高度**：列表态使用纵向 `scroll-view` 时，父级须 **`height` + `min-height`（如 `100vh`）**，避免仅 `min-height` 导致子级 `flex:1` + `height:0` 在真机高度为 0。

## API 与运行时

- **`createSelectorQuery().in(instance)`**、`getCurrentInstance`、`enhanced` 等与端强相关的 API，改完后应用 **`npm run build:mp-weixin`** 并关注真机差异。
- **样式**：注意 `gap`、`aspect-ratio`、部分 `calc` 等在低版本基础库的表现；关键视觉用已验证的写法兜底。

## 自检

- 改动涉及 **页面结构、滚动、条件渲染、分包页** 时：**本地执行 `npm run build:mp-weixin`**，并在说明中提醒 **真机预览** 验证。
